#include <bits/stdc++.h>
using namespace std;

int main()
{
    string str = "whyrawhyra";
    string smoll = "ra";

    int index = str.find(smoll);

    cout << "Which index do we start at " << index;

    return 0;
}